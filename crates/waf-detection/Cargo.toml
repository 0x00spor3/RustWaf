[package]
license = "Apache-2.0"
name = "waf-detection"
version = "0.1.0"
edition = "2021"

[dependencies]
waf-core = { path = "../waf-core" }
waf-normalizer = { path = "../waf-normalizer" }
regex = "1"
tracing = "0.1"

[dev-dependencies]
waf-pipeline = { path = "../waf-pipeline" }
bytes = "1"
